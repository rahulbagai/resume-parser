{
	"name": "Python 3.12",
	"image": "mcr.microsoft.com/devcontainers/python:3.12",

	"customizations": {
		"vscode": {
			"extensions": [
				"ms-python.python",
				"ms-python.vscode-pylance"
			]
		}
	},

	"postCreateCommand": "pip install --upgrade pip && pip install -e '.[dev]' && python -m spacy download en_core_web_sm"
}
